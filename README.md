# 🐈‍⬛ AmigochReborn

Telegram бот с ИИ (GitHub Copilot GPT-4o-mini), который может:
- Отвечать на вопросы через AI
- Анализировать изображения
- Генерировать картинки (pollinations.ai)
- Собирать обратную связь

---

## 📱 Команды для пользователей

| Команда | Что делает |
|---------|-----------|
| `/start` | Запуск бота |
| `/mode` | Переключись между ИИ-чатом и фидбеком |
| `/clear` | Очисти историю |
| `/uptime` | Когда включен бот |
| `/admins` | Контакты админов |
| `/generate текст` | Сгенерируй картинку |

## 🛠️ Для администраторов

| Команда | Что делает |
|---------|-----------|
| `/ap` | Админ-панель |
| `/stop` | Выключи бота |
| `/restart` | Перезапусти бота
| `/fb_ban <id>` | Забань пользователя в фидбеке |
| `/fb_unban <id>` | Разбань пользователя в фидбеке |

---

## ⚙️ Как это работает

- Пиши боту любое сообщение → он ответит с помощью GPT-4o-mini
- Кидай картинку → бот проанализирует
- Команда `/mode` → переключишься на отправку фидбека админам
- История сохраняется в SQLite для контекста

---

## 📝 Файлы проекта

```
main.py        — запуск бота
handlers.py    — команды и обработка сообщений
database.py    — база данных (история, блокировки)
utils.py       — вспомогательные функции
prompt.txt     — персональность бота
```

## 🚀 Как запустить свой бот

### Требования
- Python 3.8+
- Интернет (данунахуй)

### Установка нативно

#### 1. **Клонируй репо**
```bash
git clone https://github.com/IgorVasilekIV/AmigochReborn.git
cd AmigochReborn
```

#### 2. **Установи зависимости**
```bash
pip install -r requirements.txt
```

#### 3. **Перенеси ключи**
Переименуй `env-example` → `.env` и заполни:

```
BOT_TOKEN=YOUR_BOT_TOKEN_HERE
COPILOT_API_KEY=YOUR_GITHUB_KEY_HERE
```

###### **Где взять ключи:**
 
- **BOT_TOKEN** → напиши боту [@BotFather](https://t.me/BotFather) и создай бота (`/newbot`) либо, получи ключ от готового (`/token`)

- **COPILOT_API_KEY** → Создай ключ на [GithubPAT](https://github.com/settings/personal-access-tokens), `Generate new token`. Вводим любое название ключа. В `Expiration` ставьте удобную себе дату когда у ключа закончится срок действия, либо навсегда но это не безопасно. В `Permissions` ищите `Copilot Requests`

#### 4. **Запусти бот**
```bash
python -m start
```


### 🐳 Запуск / установка в Docker

#### Ну для начала нам нужен сам докер 🤔

##### **Установка**
```bash
sudo apt install docker docker-buildx -y
```

##### **Сборка и запуск образа**
```bash
docker build -t ar_bot .
docker run --env-file .env ar_bot
```

> Название образа может быть любым, но для удобства пусть будет `ar_bot` 🤔
---

## **Уже известные мне проблемы**

> - *Q*: После перезапуска не работает ИИ
> - *A*: Напишите `/start` в бота и он заработает у **вас**

> - *Q*: Нету обработки медиа кроме фото! 
> - *A*: Будет добавлено позднее

> - ~~*Q*: хуй?~~
> - ~~*A*: нету хуя 😡~~
 
# Бла бла бла и тд.

> **Разработчик**: [@IgorVasilekIV](https://t.me/IgorVasilekIV) / [Gemini](https://gemini.google.com)

> **Создатель ридми**: [Github Copilot](https://github.com/copilot) (похуй, пусть будет)

---
